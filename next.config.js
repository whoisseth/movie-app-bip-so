/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  images: {
    // domains: ['static.tvmaze.com'],
    domains: ['image.tmdb.org', ' movie-task.vercel.app'],
  },
}
